%p
  %b Название:
  = @script.name
%p
  %b Порядок вопросов:
  = @script.order
%p
  %b Страница перенаправления:
  = @script.redirect_page
%p
  %b Время теста:
  = @script.time
%p
  %b Заблокирован?:
  = @script.blocked ? "ДА" : "НЕТ"
%p
  %b Тест:
  = link_to @script.test.name, @script.test
%p
  %b Вывод результатов тестирования:
  - if @script.procents or @script.passed_themes or @script.detalized
    %pre
      = ""
      - if @script.procents
        %b Процент сданых вопросов
      - if @script.passed_themes
        %b Процент сданых тем
      - if @script.detalized
        %b Детальный отчет
%p
  %b Темы:
  -@script.themes.each do |theme|
    = link_to theme.name, theme
%p
  %b Подтемы:
  -@script.sub_themes.each do |sub_theme|
    = link_to sub_theme.name, sub_theme

= link_to 'Редактировать', edit_script_path(@script), :class => 'btn'
= link_to 'Назад', scripts_path, :class => 'btn'
