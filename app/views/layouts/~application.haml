!!!
%html
  %head
    %title Редактор сценариев тестирования
    = stylesheet_link_tag 'reset', 'application', 'main', '2col', 'style', 'mystyle', 'scaffold'
    =javascript_include_tag :defaults,'jquery.tokeninput','js/highcharts','js/modules/exporting'
    = csrf_meta_tag
  %body
    #main
      = render 'layouts/tray'
      %hr.noscreen
      = render 'layouts/menu'
      #cols.box
        = render 'layouts/aside'
        %hr.noscreen
        #content.box
          -unless notice.blank?
            .msg.done=notice
          -unless flash[:error].blank?
            .msg.error=flash[:error]
          = yield
      %hr.noscreen
      = render 'layouts/footer'
